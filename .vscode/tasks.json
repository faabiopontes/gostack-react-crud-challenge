{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "json-server",
      "type": "shell",
      "command": "yarn && yarn json-server server.json -p 3333"
    },
    {
      "label": "start",
      "type": "shell",
      "command": "yarn && yarn start"
    },
    {
      "label": "test",
      "type": "shell",
      "command": "yarn && yarn test"
    }
  ]
}
